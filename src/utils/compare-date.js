/**
 * Сравнение двух дат их вычитанием
 * @param {Date} date1 - первая дата
 * @param {Date} date2 - вторая дата
 * @return {Number} - разница дат
 */
export default function compareDate(date1, date2) {
  return date1 - date2;
}
